## Webpack Bundler Test

This directory is designed to test that a simple Webpack configuration can bundle `@gentrace/node` correctly.

### Instructions

```
npm run build
```

Generated bundled assets are created into the `dist` folder.